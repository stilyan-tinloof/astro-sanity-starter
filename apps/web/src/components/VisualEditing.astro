---
import { VisualEditingComponent, type VisualEditingOptions } from './VisualEditingComponent';

interface Props extends VisualEditingOptions {
  enabled?: boolean;
}

const { enabled, zIndex } = Astro.props;
---

{enabled ? <VisualEditingComponent client:only="react" zIndex={zIndex} /> : null}
